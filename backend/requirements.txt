flask==3.0.3
flask-cors==4.0.1
groq==0.9.0
httpx<0.28.0
python-dotenv==1.0.1
APScheduler==3.11.2
gTTS==2.5.4
gunicorn==22.0.0
# PostgreSQL + vector search
psycopg2-binary==2.9.9
# Multilingual sentence embeddings (enables full pgvector semantic search)
# Supports Telugu + English. Downloads ~470 MB model on first run.
# Remove this line to run without vector search (text-search fallback activates automatically).
sentence-transformers==2.7.0
